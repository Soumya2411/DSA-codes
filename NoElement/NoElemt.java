class Solution {
    public int removeElement(int[] nums, int val) {
        int l=nums.length;
         ArrayList<Type> list = new ArrayList<>();

        for(int i=0;i<l;i++)
        {
            
            if(nums[i]!=val)
        arr[i]=nums[i];
        
        }
        return arr;

    }

}
