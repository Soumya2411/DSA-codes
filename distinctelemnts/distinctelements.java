package distinctelemnts;

import java.util.HashSet;
import java.util.Set;

public class distinctelements {
    //in a set hashset every element can be stored only once//
    int count distinct[int[] a]{
        Set<E> <Integer> set=new HashSet<>();
        for(int element: a){
            set.add(element); 
            //ina a set every element can be added only once so to check distinct elemnts we are adding it in the set and then taking back the size//
        }
        return set.size();
    }


}
